# Versions used for the numbers in the manuscript (macOS 26, Apple silicon, CPython 3.14.6).
# These pins record the original environment; supported dependency ranges in
# pyproject.toml are not a claim that every allowed version was tested.
# Additional verified environments and their scope are recorded in validation/.
numpy==2.5.2
scipy==1.18.1
sympy==1.14.0
mpmath==1.3.0
matplotlib==3.11.1
pytest>=8      # optional; python tests/test_reproduce.py runs the same checks without it
