# Versions used for the numbers in the manuscript (macOS 26, Apple silicon, CPython 3.14.6).
# Any numpy>=1.24 / scipy>=1.11 / sympy>=1.12 / mpmath>=1.3 / matplotlib>=3.7 reproduces them
# to the tolerances stated in tests/test_reproduce.py.
numpy==2.5.2
scipy==1.18.1
sympy==1.14.0
mpmath==1.3.0
matplotlib==3.11.1
pytest>=8      # optional; python tests/test_reproduce.py runs the same checks without it
