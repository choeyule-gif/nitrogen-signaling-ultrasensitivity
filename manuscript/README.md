# Submission manuscript

The main manuscript follows the PLOS LaTeX template and contains text, references and captions, with main figures uploaded separately. The complete initial-submission file set is attached to GitHub release v1.1.0 as `PLOS_CB_submission_v1.1.0.zip`.

Compile `manuscript_submission.tex`, `S1_Appendix.tex`, `cover_letter.tex` or individual `S1_Fig.tex` through `S10_Fig.tex` with pdfLaTeX twice. Supporting figure inputs are in `figure_inputs/`.

The paper has 31 cited references, with bibliography ordered by first citation. The Author summary is 157 words. No journal acceptance or completed submission is implied.

## Archive status

Scientific code release v1.1.0 is public. Zenodo's release hook returned server timeouts (500/504), including a redelivery, so a new version DOI could not be verified. The manuscript accurately distinguishes the public v1.1.0 GitHub release from the baseline-only Zenodo DOI 10.5281/zenodo.22731769. Update this paragraph and the data availability statement only after the new Zenodo record is confirmed.
